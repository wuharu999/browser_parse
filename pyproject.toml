[project]
name = "robot-log-workbench"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn>=0.30,<1", "httpx>=0.28,<1"]

[project.optional-dependencies]
worker = [
    "cubesandbox==0.7.0",
]

[tool.uv]
package = false
